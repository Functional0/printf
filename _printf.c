#include "main.h"

/**
 * _printf: A function to print out the supplied arguments
 * according to some specified formatters.
 *
 * @format: A character string containing all the desired characters
 *
 * Return: Number of characters printed
 */
int _printf(const char *format, ...)
{
    return 0;
}